<src> : include
<src/coqdoc.{native,byte}>: package(str), package(unix), use_clib, rectypes, use_lib

<src/*.ml{,i,y}>: package(str), package(unix), use_clib, rectypes, use_lib
true: rectypes, annot, debug

